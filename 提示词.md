# 提示词（AI 编码助手使用）

> 本文件是「市场追踪助手」仓库的 AI 编码提示词。使用 AI 编码助手（Codex、Cursor、Copilot 等）
> 修改本仓库时，请把本文件与根目录 `AGENTS.md` 一起阅读，并视为最高优先级约束。

## 一、硬性修改范围（强制，不可违反）

1. **只允许修改前端代码**，即 `front/` 目录（微信原生小程序，TypeScript + MobX）。
2. **禁止修改后端代码**，即 `backend-java/` 目录（Spring Boot + Java + PostgreSQL）下的任何文件，
   包括 `src/`、`pom.xml`、`Dockerfile`、`scripts/` 等。
3. 仓库根目录文档（`README.md`、`AGENTS.md`、`docs/` 等）可修改，但需保持内容与仓库现状一致。
4. 如果任务需要后端接口或数据变更，**不要自行改后端**，应明确指出所需变更并交由用户处理。

## 二、项目概况

- 前端 `front/`：微信原生小程序「市场追踪助手」，TypeScript 5.8+（strict 模式）、MobX 状态管理、
  tdesign-miniprogram 组件库；单测使用 Node 内置 test runner，Lint 使用 ESLint 9 flat config + Prettier。
- 后端 `backend-java/`：Spring Boot + Java + PostgreSQL，通过 HTTP 提供行情、K 线、新闻等数据接口。
- 小程序支持**浅色 / 深色双主题**，这是核心体验。任何 UI 改动必须保证两种主题下都可读，
  详见 `AGENTS.md` 的「主题兼容规范」（强制）。

## 三、前端工作规范（摘要，详见 AGENTS.md）

- 主题单一数据源：`front/stores/settings.store.ts` 的 `rootStore.settings.theme`；
  切换主题一律调用 `rootStore.settings.setTheme(theme)`，不要直接改持久化值或 `app.globalData.theme`。
- 页面用 `front/utils/theme.ts` 的 `bindTheme(target)` / `unbindTheme(target)` 订阅主题，
  根节点写 `class="page theme-{{theme}}"`。
- 自定义组件样式隔离：深色样式必须写在组件自身 wxss，组件根节点加 `dark` 类，
  Canvas 绘制（K 线图、分时图）配色必须随主题切换。
- 新增 / 修改 UI 后，必须手动切一次深色主题检查对比度与可读性。
- 涉及前端依赖变更时，先执行 `npm view <pkg> version` 确认最新稳定版，不要凭记忆写版本号。

## 四、提交前检查

- 在仓库根目录运行 `pnpm check`（test + type-check + lint + format:check），确保全部通过。
- 也可单独运行：`pnpm test` / `pnpm type-check` / `pnpm lint` / `pnpm format:check`。
- 只提交与任务相关的文件，避免夹带无关改动（如 lockfile 的意外变更）。
