server:
  port: 18487

spring:
  application:
    name: stock-backend
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 25
      minimum-idle: 5

app:
  collector:
    auto-full: false
    sample-size: 20
    run-sample-on-start: false
  jwt:
    secret: ${JWT_SECRET}
    expire-hours: ${JWT_EXPIRE_HOURS:24}
  wechat:
    app-id: ${WECHAT_APP_ID}
    app-secret: ${WECHAT_APP_SECRET}
  sina:
    # 值由 ConfigLoader 从 config.yaml 的 stock.sina.* 读取（默认值兜底）
    rate-limit-seconds: ${SINA_RATE_LIMIT:1.0}
    max-retries: ${SINA_MAX_RETRIES:3}
    timeout-seconds: ${SINA_TIMEOUT:30}
    user-agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
    referer: "https://finance.sina.com.cn"
