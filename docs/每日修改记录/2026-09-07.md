# 接口修改记录 · 2026-09-07

> 说明：本文件只记录**对外 HTTP 接口**（`/api/v1/**` controller 层契约）的变更；
> 内部实现文件仅列在「关联文件」，不影响接口契约的内部重构归入「备注」。

## 一、新增接口

（无）

## 二、修改接口

（无）

## 三、删除接口

（无）

## 备注

- **门户网站 SEO 优化（纯前端 `portal/`，无 `/api/**` 接口契约变化）**：
  - `Seo.astro`：新增 `noindex` 支持（404 页输出 `robots: noindex` 且不再输出
    canonical / OG url）；普通页显式输出 `robots: index, follow`；
    `canonical` 自动补全尾斜杠与站点 `trailingSlash: 'always'` 对齐；
    `og:locale` 由 `zh-CN` 修正为 `zh_CN`；新增 `og:image:alt` / `twitter:image:alt`。
  - `robots.txt` 由静态文件改为动态生成（`portal/src/pages/robots.txt.ts`），
    sitemap 地址从 `SITE_URL` 推导，替换真实域名后无需再手工同步；
    删除 `portal/public/robots.txt`。
  - `site.ts` / `Base.astro`：新增 `SEO_VERIFICATION`（google / baidu / bing 验证
    token）配置入口，填 token 后每个页面 `<head>` 输出对应验证 meta，留空零输出。
  - FAQ 结构化数据与页面可见问答严格一致：首页 FAQPage 收缩为可见的 4 条；
    金价落地页新增「金价常见问题」可见 FAQ 区块（2 条问答与 FAQPage 一致）。
  - 落地页（全球行情 / 金价 / 日韩行情 / 有色金属 / 财经资讯）卡片主标题由
    `h3` 提升为 `h2`（语义化标题层级），新增 `.card-title` 样式保持视觉一致；
    各页正文补充指向相关行情页的互链锚文本。
  - 关联文件（`portal/`）：`src/components/Seo.astro`、`src/components/QrCta.astro`、
    `src/layouts/Base.astro`、`src/config/site.ts`、`src/pages/robots.txt.ts`（新增）、
    `src/styles/global.css`、`src/pages/404.astro`、`src/pages/index.astro`、
    `src/pages/{gold,global,asia,metals,news}/index.astro`、
    `public/robots.txt`（删除）、`README.md`。
  - 本地验证：`astro check` 0 错误；`astro build` 通过；产物 head 复查通过；
    Lighthouse（desktop）SEO 100。
  - `SITE_URL` 已按用户确认为线上正式域名 `https://stock-offical.guyu.org.cn`
    （原占位 `portal.example.com` 导致线上 canonical / robots sitemap 指向错误，本次一并修正）。
  - 百度站长验证 token（`codeva-x2qvLaNqE0`）与 Google 站长验证 token
    （`7eCgPXA5vtlMCHYaCp908NnH5MMRViKB3svxt-KbpTY`）均已填入 `SEO_VERIFICATION`
    （重新构建后每个页面 `<head>` 依次输出 `google-site-verification` /
    `baidu-site-verification`，待部署上线后在对应站长平台点击验证）。
  - **线上已确认为新构建并完成百度收录推送（运营操作，无代码变更）**：线上
    `https://stock-offical.guyu.org.cn` 的 canonical / robots meta /
    `baidu-site-verification` 均已生效；使用百度普通收录 API
    （`data.zz.baidu.com/urls`）推送全站 9 个页面，返回 `success:9, remain:1`
    （API 与手动提交共享配额，当日剩余 1 条，不可累计）。
  - 待用户操作（无法在代码内完成）：百度 / Google 平台点击「验证」；Bing 验证 token
    填写（如需要）；百度 sitemap 提交（`/sitemap-0.xml`，配额独立）；小程序码占位图替换。
