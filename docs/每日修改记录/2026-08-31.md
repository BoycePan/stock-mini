# 接口修改记录 · 2026-08-31

> 说明：本文件只记录**对外 HTTP 接口**（`/api/v1/**` controller 层契约）的变更；
> 内部实现文件仅列在「关联文件」，不影响接口契约的内部重构归入「备注」。

## 一、新增接口

（无）

## 二、修改接口

（无）

## 三、删除接口

（无）

## 备注

- **广告位调整（纯前端，无 `/api/**` 接口契约变化）**：
  - 分时页（`front/packageQuote/pages/minute/index`）广告位从内容顶部移到
    **分时图下方**（panel 之后、免责声明之前），location 仍为 `minute-detail`；
  - 美股TOP100（`front/packageQuote/pages/us-top100/index`）**新增广告位**，
    location 为 `us-top100`，展示在列表下方（免责声明之前）。
- **后端配合项（数据配置，非接口契约变化）**：`us-top100` 是新广告位标识，
  需要后端在 `app_config`（`cfg_type='display'`）的 `adConfig.bannerAd` 中新增一条
  `{ "location": "us-top100", "unit-id": "<流量主广告位ID>", "status": true }`，
  配置到达前前端不渲染该广告位（不影响布局）。
- 关联文件（前端）：`front/packageQuote/pages/minute/index.wxml`、
  `front/packageQuote/pages/us-top100/index.{wxml,json}`、
  `front/types/system.ts`（location 注释）、`front/components/ad-banner/index.ts`（placement 注释）。
