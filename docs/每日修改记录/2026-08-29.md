# 接口修改记录 · 2026-08-29

> 说明：本文件只记录**对外 HTTP 接口**（`/api/v1/**` controller 层契约）的变更；
> 内部实现文件仅列在「关联文件」，不影响接口契约的内部重构归入「备注」。

## 一、新增接口

（无）

## 二、修改接口

（无）

## 三、删除接口

（无）

## 备注

- **纯前端改动，无 `/api/**` 接口契约变化**：`packageQuote/pages/us-top100/index`
  列表行新增公司 logo（外链图片）。
- 新增外部数据源：Financial Modeling Prep 公开图片 CDN
  `https://financialmodelingprep.com/image-stock/{SYMBOL}.png`（免费、无 Key），
  2026-08-29 对东财美股 TOP100 全部代码逐条实测，别名映射 `BRK_A→BRK.A`、`BRK_B→BRK-B` 后
  100/100 覆盖。详见 `docs/frontend-data-sources.md` §7。
- **上线需配置**：小程序后台「downloadFile 合法域名」需加入 `financialmodelingprep.com`，
  否则线上 `<image>` 加载失败（失败时页面已兜底显示公司名首字，不影响功能）。
- logo 黑白分档清单（`front/utils/us-stocks.ts` 的 `US_WHITE_LOGO_CODES` /
  `US_DARK_LOGO_CODES`）按当前 FMP 图片核验，上游换图时需复核。
- 关联文件（前端）：`front/utils/us-stocks.ts`、
  `front/packageQuote/pages/us-top100/index.{ts,wxml,wxss}`、`docs/frontend-data-sources.md`。
