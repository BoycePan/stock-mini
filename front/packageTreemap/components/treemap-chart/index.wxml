<view class="treemap-chart {{ theme === 'dark' ? 'theme-dark' : '' }}" style="height: {{ height > 0 ? height + 'px' : '100%' }};">
  <canvas
    type="2d"
    id="treemap-canvas"
    class="treemap-canvas"
    bindtap="onTap"
  ></canvas>
</view>