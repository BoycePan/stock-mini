/**
 * 本小程序的分端标识（source）。
 * 后端 app.wechat.apps / app_config / notice / users 均按此分端读写；
 * 与 application.yml 的 app.wechat.default-source（shiChang-tracker）保持一致。
 */
export const APP_SOURCE = 'shiChang-tracker'
